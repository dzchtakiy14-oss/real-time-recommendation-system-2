global:
  scrape_interval: 5s

rule_files:
  - "alert.rules.yml"

scrape_configs:
  - job_name: "fastapi"
    static_configs:
      - targets: ["api:8000"]
  
  - job_name: "redis"
    static_configs:
      - targets: ["redis-exporter:9121"]